# Skills App API Route Specs

`component.spec.json` is the machine contract for the Skills App API route crate. This crate owns
App API handlers, request mapping, the raw business router, and its route manifest. API assemblies
compose that contribution; gateways own the Web Framework pipeline and infrastructure routes.
