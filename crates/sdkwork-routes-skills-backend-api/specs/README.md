# Skills Backend API Route Specs

`component.spec.json` is the machine contract for the Skills Backend API route crate. This crate
owns Backend API handlers, request mapping, the raw business router, and its route manifest. API
assemblies compose that contribution; gateways own the Web Framework pipeline and infrastructure
routes.
